#spring:
#  data:
#    mongodb:
#      host: localhost
#      port: 27017
#      database: bookdb
server:
  port: 0
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    hostname: localhost